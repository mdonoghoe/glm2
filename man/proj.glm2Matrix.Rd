% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proj.glm2Matrix.R
\name{proj.glm2Matrix}
\alias{proj.glm2Matrix}
\title{Projections of glm2Matrix models}
\usage{
\method{proj}{glm2Matrix}(
  object,
  onedf = FALSE,
  unweighted.scale = FALSE,
  column.order = c("fit", "original"),
  ...
)
}
\arguments{
\item{object}{an object of class \code{glm2Matrix} as returned by \code{\link{glm2}}.}

\item{onedf}{A logical flag. If \code{TRUE}, a projection is returned for
all the columns of the model matrix. If \code{FALSE}, the single-column
projections are collapse by terms of the model (as represented in the
analysis of variance table).}

\item{unweighted.scale}{If the fit producing \code{object} used weights, this
determines if the projections correspond to weighted or unweighted observations.}

\item{column.order}{\code{"fit"} uses the sparse QR ordering from
the fitted model. \code{"original"} recomputes the final weighted 
least-squares QR in the original reduced model-matrix column order 
(via \code{Matrix::qr(..., order = 0L)}); this may be much slower for 
large sparse problems.}

\item{...}{Swallow and ignore any other arguments.}
}
\value{
See documentation for \code{\link[stats]{proj}}.
}
\description{
An implementation of \code{\link[stats]{proj}}
for GLMs fit using \code{glm2(..., method = "\link{glm.fit2.Matrix}")}
}
\seealso{
\code{\link[stats]{proj}}
}
\author{
Adapted by Mark Donoghoe from the code for \code{proj} and \code{proj.lm}.
}
